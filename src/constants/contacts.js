import path from 'node:path';

export const PATH_DB = path.resolve('src', 'db', 'db.json');

// const pathToWorkDir = path.join(process.cwd());
// export const PATH_DB = path.join(pathToWorkDir, 'src', 'db', 'db.txt');

// console.log(PATH_DB);
